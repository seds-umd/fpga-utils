# Utilities

`waves` command - run `waves` in a test dir to run gtkwave. It looks for `sim_build/*.fst` and `*.gtkw` in the the current dir and uses the first ones it finds.

# New Project Setup

# Development

# Common Issues

## No FST file generated

